#include "main.h"
#include <stdio.h>

/**
 * main - prints _putchar, followed by a new line.
 * Return: 0 if succesful, otherwise non-zero
*/
int main(void)
{
char word[] = "_putchar";
printf("%s\n", word);

return (0);
}
